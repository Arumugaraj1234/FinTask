enum StateRendererType {
  popUpLoading,
  popUpError,
  popUpSuccess,
  fullScreenLoading,
  fullScreenError,
  emptyScreen,
  content
}

enum StateRendererAction { ok, retry, success }

enum SortType { byName, byAge, byCity }
